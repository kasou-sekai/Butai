# Butai 产品需求与 MVP 实施规格

> 文档用途：作为 Butai macOS 应用进入设计与编码阶段的主需求文档。
>
> 产品名：Butai（舞台）  
> 品牌语：Set the stage for your work.  
> 中文语：让每个桌面，各就各位。  
> 文档版本：0.1（MVP）  
> 日期：2026-08-11

> 实现状态说明（2026-08-21）：本文保留最初 MVP 需求作为产品基线。第 2、3、5 节中“禁用私有 API”和“不支持全屏 Space”的早期边界，已被 `IMPLEMENTATION_DECISION.md` 及后续实现取代；当前版本使用隔离的私有 CGS/SkyLight 拓扑读取，并支持识别和导航全屏应用 Space。尚未实现的项目以 `AUDIT_2026-08-12.md` 的“Known functional gaps”为准，不能视为当前能力。

---

## 1. 产品定义

Butai 是 macOS 原生 Spaces（多个桌面）的增强工具。它不替换 Mission Control，不改变用户已经熟悉的触控板和系统快捷键逻辑，而是在原生桌面之上增加：

1. 工作区命名；
2. 顶部常驻的当前工作区标识；
3. 工作区快速切换；
4. 应用、窗口、文件、目录和 URL 预设；
5. 缺失窗口补全和窗口布局恢复。

核心产品隐喻：每个工作区都是一个“舞台”，应用和窗口在进入舞台时各就各位。

Butai 的首要原则是保护用户当前工作。任何恢复操作默认只能打开缺失内容、移动或缩放已匹配窗口，不得擅自关闭窗口、退出应用或丢弃未保存内容。

---

## 2. 产品目标

### 2.1 MVP 目标

- 完整保留 macOS 原生桌面切换方式；
- 为 1–9 个普通桌面建立稳定的逻辑名称和顺序；
- 在屏幕顶端中央持续显示当前工作区名称；
- 用户切换桌面后，浮窗及时显示左侧、当前和右侧工作区名称；
- 允许通过点击和全局快捷键切换工作区；
- 支持为每个工作区配置应用、文件、目录、URL 和窗口布局预设；
- 首批可靠支持通用应用、Finder、Visual Studio Code 和 Microsoft Edge；
- 允许同一个多窗口应用在多个工作区分别拥有独立窗口；
- 提供“补全预设”和“恢复布局”两个安全级别不同的操作；
- 对权限缺失、桌面映射失准和应用适配失败提供明确反馈。

### 2.2 非目标

MVP 不做以下事情：

- 不取代 Mission Control；
- 不使用私有 SkyLight/CGS API 直接管理 Space ID；
- 不创建或删除系统 Spaces；
- 不承诺修改系统原生 Desktop 名称；
- 不关闭预设以外的窗口；
- 不自动退出应用；
- 不恢复应用内部未公开的完整状态；
- 不支持全屏应用 Space、Split View 或 Stage Manager；
- 不保证多显示器同时导航和每台显示器独立浮窗；但工作区名称、预设和校准状态必须按显示器严格隔离；
- 不保证 Mac App Store 发布，MVP 优先采用签名、公证后的直接分发。

---

## 3. 平台范围和运行前提

### 3.1 MVP 支持范围

- macOS 14 Sonoma 或更高版本；
- Apple Silicon 为主要支持平台；如无额外成本，可同时构建 Intel；
- 单显示器为主要运行形态；多显示器至少必须保证显示器绑定配置不会串用或丢失；
- 1–9 个普通桌面；
- 菜单栏可见；
- Stage Manager 关闭；
- 不将全屏应用和 Split View 计入工作区序号。

### 3.2 必须引导用户完成的系统设置

- 关闭“根据最近的使用情况自动重新排列 Spaces”；
- 建立与 Butai 工作区数量相匹配的普通桌面；
- 授予辅助功能权限，用于读取、聚焦、移动和缩放其他应用窗口；
- 授予实现全局快捷键所需的输入监听权限；
- 如需要发送系统桌面快捷键，授予对应的事件发送权限。

应用必须提供“系统兼容性检查”页面，逐项显示上述状态和修复入口。

---

## 4. 关键术语

- **Space / 系统桌面**：macOS Mission Control 管理的原生桌面。
- **Stage / 工作区**：Butai 对某个顺序位置上的普通系统桌面的逻辑映射。
- **Scene / 预设**：某个工作区期望打开的资源及窗口布局集合。
- **Preset Item / 预设项目**：一个应用、文件、目录、URL 或应用专用打开动作。
- **补全预设**：只打开缺失项目，并聚焦或复用已经匹配的窗口。
- **恢复布局**：在补全预设基础上，恢复窗口位置、大小和可选的前后顺序。
- **重新校准**：重新确认当前系统桌面对应的 Butai 工作区序号。

产品界面以“工作区”“预设”“应用与窗口”等直白术语为主；Stage/Scene 隐喻主要用于品牌表达，避免妨碍首次理解。

---

## 5. 系统技术边界

macOS 的公开 API 可以通知应用发生了 Space 切换，但不会公开提供当前 Space 的稳定 ID、名称或逻辑序号，也没有公开 API 可以直接跳转到任意 Space ID。因此：

1. Butai 保存“逻辑工作区序号 → 名称和预设”的映射；
2. Butai 维护一个当前逻辑序号；
3. 收到 Space 切换通知后，根据已发出的切换指令或相邻切换方向更新逻辑序号；
4. 如果发生无法判断来源的桌面变化，应用必须检测可能的失准，并允许用户一键重新校准；
5. 禁止把非公开 Space ID 当作持久化主键；
6. 禁止以依赖私有框架为 MVP 前提。

这一限制必须体现在代码架构中：Space 导航和工作区模型之间通过协议隔离，以便未来选择性增加更强的导航实现，而不污染预设和 UI 模块。

---

## 6. 用户流程

### 6.1 首次启动

1. 显示产品简介：Butai 增强而不替代原生 Spaces；
2. 检查 macOS 版本、显示器数量、Stage Manager 和自动重排设置；
3. 请求辅助功能及输入相关权限，并解释用途；
4. 用户选择工作区数量，默认 4 个；
5. 用户确认已建立相同数量的普通桌面；
6. 用户从当前桌面开始执行校准；
7. 创建默认名称：工作区 1、工作区 2……；
8. 展示如何重命名、配置预设和使用快捷键；
9. 显示顶部浮窗和菜单栏图标。

首次启动流程必须允许跳过预设配置，先使用纯命名和切换功能。

### 6.2 日常切换

用户可以通过以下方式切换：

- 原生触控板横向滑动；
- Control+左/右方向键；
- Mission Control；
- 点击 Butai 浮窗展开后的工作区名称；
- Caps Lock+1…9；
- 用户自定义的替代快捷键。

无论通过何种方式完成切换，Butai 都应在收到系统 Space 变化通知后更新 UI。无法可靠判断目标序号时，不应伪装成准确状态，应提示校准。

### 6.3 创建预设

用户进入某工作区后，可选择：

- 从当前已打开窗口生成预设；
- 手动添加应用、文件、文件夹或 URL；
- 添加应用专用项目，例如“用 VS Code 新窗口打开目录”；
- 记录可匹配窗口的位置和尺寸；
- 为每个项目指定“必须新建窗口”或“优先复用已有窗口”。

### 6.4 补全预设

1. 枚举当前可访问窗口；
2. 将窗口与预设项目进行匹配；
3. 对已经满足的项目不做重复打开；
4. 按预设适配器打开缺失项目；
5. 等待窗口出现并重新匹配；
6. 输出成功、跳过、失败三类结果；
7. 不移动已匹配窗口，除非项目配置要求首次定位。

### 6.5 恢复布局

1. 先执行补全预设；
2. 对匹配窗口恢复位置和大小；
3. 尺寸需要根据当前屏幕可用区域校正；
4. 不得把窗口移动到菜单栏或 Dock 后方；
5. 无法调整的窗口标记为失败，但继续处理其他项目；
6. 默认不关闭额外窗口；
7. 恢复结束后展示简短摘要。

---

## 7. 顶部浮窗规格

### 7.1 位置与层级

- 使用无边框透明浮动窗口，而非依赖 NSStatusItem 居中；
- 水平位置为主屏幕中央；
- 默认位于菜单栏下沿或不与刘海冲突的安全区域；
- 设置为可加入所有 Spaces；
- 不参与普通窗口切换；
- 默认不抢键盘焦点；
- 普通状态下鼠标事件区域必须严格贴合可见内容，不能形成大面积隐形遮挡；
- 支持用户调整垂直偏移；水平位置固定居中，并提供恢复默认位置。

### 7.2 折叠状态

默认只显示当前工作区名称，可选显示序号：

```text
开发 · 2
```

样式要求：

- 接近 macOS 原生材质；
- 深浅色模式自适应；
- 高对比度和减少透明度设置下仍可读；
- 名称过长时截断，悬停显示完整名称；
- 浮窗可以在设置中隐藏；
- 全屏应用场景 MVP 可自动隐藏或明确标记为不支持。

### 7.3 切换反馈状态

Space 切换完成后短暂显示：

```text
设计   ←   开发   →   沟通
              2
```

- 左右边界没有相邻工作区时隐藏对应项；
- 动画时长默认 700–1200ms，可配置；
- 开启“减少动态效果”时不使用滑动动画；
- 不承诺在系统触控板切换动画开始前预测方向，MVP 以系统确认切换后更新为准。

### 7.4 悬停展开状态

鼠标进入后展开，至少提供：

- 全部工作区名称和当前状态；
- 点击任意工作区进行切换；
- 补全当前预设；
- 恢复当前布局；
- 打开当前工作区设置；
- 当前预设健康状态，例如“5/6 已就绪”；
- 映射可能失准时显示“重新校准”。

鼠标离开后延迟 250–500ms 折叠，避免从名称移动到操作按钮时意外关闭。

---

## 8. 菜单栏与设置窗口

### 8.1 菜单栏图标

顶部中央名称不是标准菜单栏项目，但应用仍需提供右侧菜单栏图标，菜单至少包含：

- 当前工作区；
- 工作区列表；
- 补全预设；
- 恢复布局；
- 保存当前状态到预设；
- 重新校准；
- 设置；
- 暂停 Butai；
- 退出。

### 8.2 设置窗口

侧边栏建议包含：

1. 工作区；
2. 预设与窗口；
3. 快捷键；
4. 浮窗；
5. 应用适配器；
6. 权限与诊断；
7. 通用。

设置修改应即时保存。关键删除操作支持撤销或二次确认。

---

## 9. 快捷键规格

### 9.1 默认快捷键

- Caps Lock+1…9：切换到第 1…9 个工作区；
- Caps Lock+R：补全当前工作区预设；
- Caps Lock+Shift+R：恢复当前工作区布局；
- 可选 Caps Lock+左/右：上一个/下一个工作区。

### 9.2 Caps Lock 行为

Caps Lock 并非普通修饰键，MVP 必须定义以下行为：

- Caps Lock 按住并与已配置键组合：作为 Butai 修饰键；
- 组合中的数字或字母事件不应继续输入前台应用；
- Caps Lock 单独短按：默认保留系统大写锁定行为；
- 需要避免按键抬起后大写锁定状态与 LED 状态错误；
- 快捷键支持禁用 Caps Lock 模式；
- 必须提供 Control+Option+数字等普通修饰键替代方案；
- 检测与其他快捷键、输入法、远程桌面和键盘重映射软件的潜在冲突；
- 权限不足时不得显示快捷键已生效。

### 9.3 桌面导航模式

优先级如下：

1. 用户启用 macOS“切换到桌面 N”系统快捷键时，使用直接目标导航；
2. 否则使用相对导航：根据当前逻辑序号发送所需次数的左/右切换，并等待每次 Space 变化确认；
3. 导航超时或观察到非预期变化时中止，并提示重新校准。

所有导航请求必须串行化，避免用户连续按键造成并发切换和状态错乱。快速连续请求可合并到最后一个目标。

---

## 10. 工作区与预设数据模型

### 10.1 Workspace

建议字段：

- `id`：Butai 内部稳定 UUID；
- `order`：1-based 逻辑序号；
- `name`；
- `color`：可选；
- `symbol`：可选 SF Symbol；
- `shortcutIndex`：1–9，可选；
- `presetID`：可选；
- `createdAt`；
- `updatedAt`。

不得保存私有系统 Space ID 作为唯一身份依据。

### 10.2 Preset

- `id`；
- `workspaceID`；
- `name`；
- `items`；
- `layoutVersion`；
- `createdAt`；
- `updatedAt`。

MVP 每个工作区可以只有一个默认预设，但数据结构应允许未来一个工作区拥有多个 Scene。

### 10.3 PresetItem

建议字段：

- `id`；
- `kind`：application、file、folder、url、vscodeFolder、vscodeWorkspace、finderFolder、edgeWindow、command；
- `applicationBundleIdentifier`；
- `applicationURLBookmark`：必要时使用安全书签；
- `resourceURLBookmark`；
- `displayName`；
- `openPolicy`：reusePreferred、newWindowPreferred、newWindowRequired；
- `matchRules`；
- `launchConfiguration`；
- `windowLayout`；
- `timeoutSeconds`；
- `enabled`；
- `sortOrder`。

### 10.4 WindowMatchRule

匹配信号可包括：

- Bundle ID，必须优先；
- 窗口标题精确、前缀、后缀或正则；
- 可访问性文档 URL；
- 资源路径；
- 浏览器 URL，仅在适配器能可靠获得时；
- 窗口角色和子角色；
- 创建顺序或保存时序号，作为弱信号。

匹配必须使用评分制，不可只依赖易变化的标题。低置信度匹配不得自动移动窗口，应要求用户确认或报告未匹配。

### 10.5 WindowLayout

- 屏幕标识，仅作参考；
- 保存时屏幕可用区域；
- 使用归一化坐标保存 x、y、width、height；
- 可选最小尺寸；
- 是否最小化；
- 是否要求前置；
- MVP 不恢复全屏和 Split View 状态。

持久化建议使用版本化 JSON 或 Codable 模型。必须支持迁移、备份、导入和导出，为后续版本保留扩展空间。

---

## 11. 应用与窗口发现

窗口发现使用辅助功能 API 为主，Core Graphics 窗口列表可用于补充识别和诊断，但不能把临时窗口 ID 当作持久主键。

窗口过滤要求：

- 排除 Butai 自己的浮窗；
- 排除菜单、工具提示、弹出层等非正常窗口；
- 默认排除无标题且尺寸异常的小窗口；
- 保留应用明确暴露的标准窗口和文档窗口；
- 单个应用辅助功能调用超时时，不得阻塞整个恢复任务；
- 对无响应应用使用有限重试和超时。

窗口操作包括：

- 激活所属应用；
- 聚焦指定窗口；
- 取消最小化；
- 修改位置和尺寸；
- 必要时执行应用菜单动作。

窗口恢复任务需要可取消，并在 UI 中显示进度。

---

## 12. 应用适配器架构

需要定义统一适配器协议，至少覆盖：

- 判断是否支持某类预设；
- 枚举和描述应用窗口；
- 计算预设项目与窗口的匹配分数；
- 打开或创建资源；
- 等待目标窗口出现；
- 聚焦目标窗口；
- 返回结构化错误和恢复建议。

适配优先级：

1. 官方公开 URL/文件打开接口；
2. 应用公开命令行接口或 Apple Events；
3. 应用专用辅助功能自动化；
4. 通用应用激活和新窗口菜单动作。

辅助功能菜单自动化必须处理本地化差异，不能只硬编码英文菜单文本；优先使用角色、快捷键或可访问性标识。

---

## 13. MVP 应用适配范围

### 13.1 通用应用

支持：

- 启动应用；
- 激活应用；
- 匹配标准窗口；
- 在应用可访问时移动和缩放窗口；
- 可选通过标准“新建窗口”动作请求新窗口。

限制：无法保证所有应用遵守新窗口请求。

### 13.2 Finder

支持：

- 打开指定文件夹；
- `newWindowRequired` 时尽力创建独立 Finder 窗口；
- 根据窗口暴露的路径或标题匹配；
- 恢复位置和尺寸。

需要测试 Finder 的标签页偏好和“在标签页中打开文件夹”设置，避免请求新窗口却被系统复用为标签页。

### 13.3 Visual Studio Code

支持：

- 新窗口打开指定文件夹；
- 新窗口打开 `.code-workspace`；
- 允许不同工作区分别存在 VS Code 窗口；
- 优先根据工作区/目录与窗口标题组合匹配；
- 恢复窗口布局。

必须考虑：

- Stable、Insiders 或用户自定义 VS Code 发行版；
- `code` CLI 不在 PATH；
- 已存在目标目录窗口时的复用策略；
- 多根工作区。

### 13.4 Microsoft Edge

支持：

- 创建新窗口并打开一个或多个 URL；
- 允许不同 Butai 工作区分别存在 Edge 窗口；
- 可选指定 Edge Profile；
- 优先复用与预设相符的现有窗口；
- 恢复窗口布局。

浏览器标签页精确恢复不是 MVP 硬性承诺。首版可以把预设 URL 作为启动输入，但不自动关闭用户后来增加的标签页。

### 13.5 ChatGPT

MVP 标记为实验性，支持级别按实际验证决定：

- 最低保证：启动或激活 ChatGPT；
- 可行时：通过可访问菜单请求新窗口；
- 可行时：分别匹配多个可访问窗口并恢复布局；
- 不承诺通过外部接口打开指定聊天；
- 不依赖未公开或不稳定的内部 API；
- 适配失败不得影响同一预设中的其他项目。

### 13.6 Shell Command（高级）

- 用户可以为预设项目配置命令和参数；
- UI 必须明确提示命令具有执行本机操作的能力；
- 不通过 shell 字符串拼接执行，优先使用可执行文件路径与参数数组；
- 保存环境变量时不得明文收集密码或令牌；
- 记录退出码和有限长度输出，便于诊断；
- 该类型可以在 MVP 后半阶段实现，不得阻塞核心功能。

---

## 14. 工作区切换后的窗口行为

点击某个工作区名称或按快捷键时，默认行为仅为“切换”，不能自动执行预设恢复，以避免每次切换都产生窗口或打断用户。

每个工作区可提供可选设置：

- 切换时什么都不做（默认）；
- 切换后仅检查并显示缺失数量；
- 切换后自动补全预设；
- 不建议 MVP 提供切换后自动恢复全部布局。

当用户点击浮窗中的某个工作区：

1. 导航到目标 Space；
2. 等待系统确认切换；
3. 更新当前名称；
4. 如启用自动检查，计算预设健康状态；
5. 不应把属于同一应用、但位于其他工作区的错误窗口拉到当前工作区。

“点击切换到所有对应窗口”在产品语义上定义为：切换到该工作区，并聚焦该工作区中与预设匹配的首选窗口；不是将其他桌面的所有相关窗口移动过来。

---

## 15. 校准和失准恢复

### 15.1 校准

校准流程至少支持：

- “将当前桌面标记为工作区 N”；
- 从 N 开始，通过相邻切换遍历并确认其余工作区；
- 检查预期的切换次数和通知次数；
- 校准结束后保存时间和环境摘要。

### 15.2 失准信号

以下情况应标记为可能失准：

- 系统桌面数量发生变化；
- 用户开启自动重新排列 Spaces；
- 导航请求与收到的通知数量不一致；
- 出现或退出全屏 Space；
- 显示器连接状态变化；
- 应用重启后无法确认当前逻辑序号；
- 用户从 Mission Control 跳转且缺少可靠方向信息。

失准时：

- 顶部名称前显示警告标识；
- 不静默猜测并持久化错误序号；
- 允许用户从浮窗或菜单栏立即执行“当前是工作区 N”；
- 预设补全和布局恢复仍可手动执行，但先提示映射风险。

---

## 16. 权限与隐私

### 16.1 权限用途

- 辅助功能：读取窗口信息，激活、聚焦、移动和缩放窗口；
- 输入监听：识别全局快捷键；
- 事件发送：触发系统桌面快捷键或必要导航；
- 文件访问：读取用户主动选择的文件和文件夹，必要时保存安全作用域书签。

### 16.2 隐私原则

- 所有预设和窗口元数据默认仅保存在本机；
- 不采集窗口内容或截图；
- 不上传文件路径、窗口标题或浏览器 URL；
- MVP 不需要账户；
- MVP 不需要云同步；
- 日志默认对可能包含用户名、路径、URL 查询参数和窗口标题的信息做脱敏；
- 诊断导出前向用户展示包含的内容；
- 权限说明必须清楚解释“为什么需要”，不得使用模糊文案。

---

## 17. 错误处理

所有预设操作产生逐项目结果：

- `ready`：已有匹配窗口；
- `opened`：成功打开；
- `restored`：成功恢复布局；
- `skipped`：用户配置禁用或当前不适用；
- `permissionDenied`；
- `applicationNotFound`；
- `resourceUnavailable`；
- `windowNotFoundAfterLaunch`；
- `windowOperationUnsupported`；
- `timedOut`；
- `cancelled`；
- `unknownError`。

一次项目失败不得终止整个预设。最终摘要示例：

```text
开发舞台已恢复：4 成功，1 已存在，1 需要处理
```

用户可以展开查看详细错误和建议。

---

## 18. 性能和可靠性要求

- 常驻空闲时 CPU 使用率应接近 0；
- 不使用高频轮询检测 Space 变化；
- 浮窗更新不应明显影响系统桌面动画；
- 收到 Space 切换通知后，名称更新目标为 200ms 内开始；
- 展开浮窗目标为 100ms 内响应；
- 窗口枚举和匹配不得阻塞主线程；
- 单个无响应应用不得阻塞超过配置超时；
- 导航和恢复任务必须支持取消；
- 应用异常退出后不得破坏 macOS 原生桌面切换；
- 配置保存采用原子写入或等价保护，避免断电导致全部配置损坏；
- 至少保留一份自动备份。

---

## 19. 无障碍与本地化

- 浮窗和设置中的所有控件应提供可访问性标签；
- 支持键盘导航；
- 支持 VoiceOver；
- 支持增加对比度、减少透明度、减少动态效果；
- 不仅用颜色表达当前状态或错误；
- MVP 至少提供简体中文和英文；
- 应用适配器不得假设目标应用菜单一定是英文；
- 工作区名称支持 Unicode、emoji 和中日韩文字。

---

## 20. 建议模块划分

以下仅规定职责，不强制具体工程实现：

- **WorkspaceStore**：工作区、预设、设置的持久化和迁移；
- **SpaceObserver**：接收系统 Space 变化和显示环境变化；
- **SpaceNavigator**：直接目标导航、相对导航、请求合并和超时；
- **CalibrationCoordinator**：校准及失准检测；
- **OverlayController**：顶部浮窗布局、展开和动画；
- **StatusItemController**：菜单栏入口；
- **ShortcutEngine**：Caps Lock 与普通快捷键；
- **PermissionCenter**：权限状态、请求和修复入口；
- **WindowDiscovery**：窗口枚举、标准化和过滤；
- **WindowMatcher**：评分式匹配；
- **PresetExecutor**：补全与恢复任务编排；
- **ApplicationAdapter**：应用适配协议；
- **FinderAdapter / VSCodeAdapter / EdgeAdapter / ChatGPTAdapter**；
- **DiagnosticsCenter**：日志、健康检查和诊断导出。

模块之间应通过明确协议通信，避免设置 UI、窗口自动化和 Space 导航互相直接依赖。

---

## 21. MVP 开发阶段建议

### Phase 0：技术验证

必须先做最小技术原型，验证：

1. 顶部浮窗在普通 Spaces 切换时的位置和视觉稳定性；
2. Space 切换通知的时序；
3. Caps Lock+数字的监听、吞噬和锁定状态；
4. 目标系统版本上的权限流程；
5. Finder、VS Code、Edge 多窗口创建；
6. 辅助功能窗口匹配、移动和缩放；
7. 全屏、Mission Control、刘海和隐藏菜单栏时的降级表现。

如果浮窗无法在切换动画中保持可接受的稳定性，应降级为“切换完成后迅速淡入”，不能为了追求绝对固定而引入私有 API。

### Phase 1：命名和切换

- 工作区数据模型；
- 首次设置和校准；
- Space 观察；
- 顶部浮窗；
- 菜单栏入口；
- 工作区重命名；
- 相邻和指定序号切换；
- 基础快捷键；
- 权限页。

### Phase 2：预设和通用窗口

- 从当前窗口生成预设；
- 通用应用、文件、文件夹、URL；
- 窗口发现和匹配；
- 补全预设；
- 布局保存和恢复；
- 结果摘要和诊断。

### Phase 3：首批适配器

- Finder；
- VS Code；
- Edge；
- ChatGPT 实验性支持；
- 适配器健康状态和失败隔离。

### Phase 4：稳定和发布

- 迁移和备份；
- 中英文；
- 无障碍；
- 性能测试；
- 崩溃恢复；
- 签名、公证、更新机制；
- 隐私说明和权限引导。

---

## 22. MVP 验收标准

### 22.1 工作区和浮窗

- 可创建、删除、重命名和排序最多 9 个工作区；
- 原生触控板、Control+方向键和 Mission Control 不受破坏；
- 普通 Space 切换后顶部名称正确更新；
- 切换反馈正确显示相邻名称；
- 浮窗在普通 Spaces 间保持相同目标坐标，无明显横向漂移；
- 鼠标悬停可展开，点击可切换；
- 刘海、隐藏菜单栏、深浅色模式有明确且可用的布局。

### 22.2 快捷键

- Caps Lock+1…9 可切换到对应工作区；
- 组合中的数字不会输入前台应用；
- Caps Lock 单独使用的行为符合设置；
- 快速连续切换不会造成并发状态错乱；
- 权限被撤销后立即显示失效状态和修复入口；
- 普通修饰键替代快捷键可用。

### 22.3 预设

- 可添加应用、文件、文件夹和 URL；
- 可从当前窗口保存预设和布局；
- 补全操作不重复打开已高置信度匹配的窗口；
- 恢复布局不会关闭额外窗口；
- 恢复布局不会丢弃未保存内容；
- 单个项目失败不影响其他项目；
- 操作结束后提供逐项目结果。

### 22.4 应用适配

- VS Code 可在至少两个工作区各自打开不同目录的新窗口；
- Finder 可在至少两个工作区打开不同文件夹窗口；
- Edge 可在至少两个工作区创建独立新窗口并打开预设 URL；
- 同一个多窗口应用的窗口不会因为 Bundle ID 相同而被错误地全部视为同一个预设项目；
- ChatGPT 若不支持指定动作，必须清晰降级，而不是静默失败。

### 22.5 校准与可靠性

- 应用重启后可以恢复工作区配置；
- 无法确认当前序号时明确显示未校准状态；
- 用户可在两步以内把当前桌面标记为指定工作区；
- 桌面数量或显示器状态改变时触发重新检查；
- 配置文件损坏时可从备份恢复或安全启动为空配置。

---

## 23. MVP 后功能池

不进入第一版承诺，但架构应避免阻塞：

- 多显示器同时导航、每台显示器独立浮窗及完整的独立 Spaces 控制；
- Stage Manager；
- 全屏与 Split View；
- 一个工作区多个 Scene；
- 自动化触发器，如连接显示器、时间、Wi-Fi 或 Focus Mode；
- 更严格的“恢复到唯一状态”，并提供关闭预览和未保存保护；
- Safari、Chrome、Terminal、iTerm2、Xcode、Obsidian、Notion 等适配器；
- 标签页级恢复；
- 工作区导入、导出和分享；
- iCloud 同步；
- AppleScript/Shortcuts/CLI 接口；
- 菜单栏和浮窗主题；
- 使用统计必须完全可选并保护隐私；
- 经过单独风险评估的增强 Space 导航后端。

---

## 24. 开发决策原则

遇到需求冲突时按以下优先级决策：

1. 不丢失用户工作；
2. 不破坏 macOS 原生桌面行为；
3. 状态诚实，无法确认时不伪装准确；
4. 使用公开、可维护的系统接口；
5. 窗口匹配宁可少自动化，也不要错误移动；
6. 核心切换功能不能被单个应用适配器拖垮；
7. 先保证单显示器和普通 Spaces 的可靠体验，再扩大范围；
8. 品牌隐喻不能降低界面可理解性。

---

## 25. 编码对话的推荐起点

下一个开发对话开始时，应先完成以下工作，而不是直接实现完整产品：

1. 检查目标目录现有 Xcode 工程和 Git 状态；
2. 确定最低 macOS 版本、Swift/SwiftUI/AppKit 混合方案和分发方式；
3. 把 Phase 0 技术验证拆成可独立运行的实验；
4. 优先验证 Space 通知、跨 Space 浮窗和 Caps Lock 事件处理；
5. 根据验证结果冻结 MVP 技术边界；
6. 再建立模块骨架、数据模型、测试策略和分阶段实现计划。

未经验证，不应预先承诺浮窗能在系统滑动动画全过程绝对静止，也不应承诺通过公开 API 获得稳定 Space ID。
